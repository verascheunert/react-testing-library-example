import { describe, it, vi } from "vitest";

describe("Images", async () => {
  it("should render a list of images");

  it("should not find images without mocked localstorage");

  it("should fetch random images from API and caches them in localStorage");

  it("handles error when fetching random images");
});
